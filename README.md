# Martoun

- **`ledgerline/`** — LedgerLine, an AI document-intake sales demo for insurance brokerages
  (React + Vite + Vercel serverless). See [`ledgerline/README.md`](ledgerline/README.md) for
  setup and deploy steps.
- Repo root — HealthReceipt, an Expo/React Native ingredient-scanner app (separate project).
