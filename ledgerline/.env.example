# Server-side only — used by api/extract.ts on Vercel. Never exposed to the browser.
# Create a key at https://platform.claude.com/ and set this in the Vercel project
# settings (Settings → Environment Variables) or in .env for `vercel dev`.
ANTHROPIC_API_KEY=
